# 0.3.0 版本 Bug 修复清单（需复测）

## 一、编译与运行环境

| 编号 | Bug 描述 | 修复内容 | 复测要点 |
|------|---------|---------|---------|
| B01 | pages.json 编码异常导致微信开发者工具编译失败 | 修复为 UTF-8 无 BOM | 微信开发者工具编译无报错 |
| B02 | CLI 项目中 uniCloud SDK 未被打包，importObject 返回 undefined | main.ts 显式引入 @dcloudio/uni-cloud，手动初始化 uniCloud | 微信小程序中云函数/云对象调用正常 |
| B03 | manifest.json 缺少 uniCloud 服务空间配置 | 添加 spaceId 和 provider 配置 | 启动时 uniCloud 初始化成功 |
| B04 | node_modules 缺少编译器模块 | 执行 npm install 安装依赖 | HBuilderX/CLI 编译正常 |

## 二、登录与注册

| 编号 | Bug 描述 | 修复内容 | 复测要点 |
|------|---------|---------|---------|
| B05 | 注册提示"用户名不合法" | 自定义 register 云函数，支持手机号+密码注册，无需验证码 | 手机号注册成功并自动登录 |
| B06 | 注册后登录提示"此账号未在该应用注册" | 注册时设置 dcloud_appid 为当前应用 | 注册后可正常登录 |
| B07 | 登录失败3次后强制要求图形验证码 | 修改 uni-id 公共模块，关闭登录验证码要求 | 登录不弹出图形验证码 |
| B08 | 登录后无家庭点返回箭头回到登录页 | 登录后用 reLaunch 清空页面栈，创建/加入家庭后自动跳转首页 | 登录后无法返回到登录页 |

## 三、家庭与成员

| 编号 | Bug 描述 | 修复内容 | 复测要点 |
|------|---------|---------|---------|
| B09 | 云函数返回格式与前端不对齐 | getMyStatus 返回 hasFamily/familyName/inviteCode，createFamily/joinFamily 返回 _id 和 name | 家庭状态获取正常，创建/加入后跳转正常 |
| B10 | 成员详情页返回按钮无响应 | 函数名不匹配（goBack vs goEdit），添加页面栈兜底 | 成员详情页返回正常 |
| B11 | 生成绑定码失败"无权操作该成员" | 本地成员未同步云端，修复 generateBindCode 支持传入 memberData 自动创建；修复函数名冲突递归调用 | 管理员为虚拟成员生成绑定码成功 |
| B12 | 保存个人档案头像时报"成员头像颜色不合法" | 自动创建成员时缺少 avatarColor 字段，添加默认值 | 保存头像后自动创建成员不报错 |

## 四、UI 与交互

| 编号 | Bug 描述 | 修复内容 | 复测要点 |
|------|---------|---------|---------|
| B13 | 微信小程序 button 文字偏左不居中 | 改用 flex 布局，清除 button 默认 ::after 边框和 padding | 所有按钮文字垂直水平居中 |
| B14 | "相册"文字 badge 挡住头像 | 改为小相机图标，无头像时占满整个头像区域，有头像时只显示图片 | 头像无遮挡，点击正常 |
| B15 | 个人信息档案关系字段每次进入都是"其他" | onShow 中增加 form.role = profile.role 回显 | 进入编辑页正确显示已保存的家庭关系 |
| B16 | 首页头像相机图标偏下，背景为橙色 | 图标居中显示，背景改为灰色 #e7e5e4 | 相机图标在头像区域居中，灰色背景 |
| B17 | 账号绑定选择器按钮文字跑到按钮上方 | 按钮改为 flex 布局居中，文本改为"选择绑定" | 按钮文字居中，点击后绑定对应账号 |
| B18 | A 账号数据在 B 账号登录后仍显示 | 登录时 uid 变化则清空上一账号 Profile 数据 | 切换账号后个人信息档案为空待填写 |

## 五、数据校验

| 编号 | Bug 描述 | 修复内容 | 复测要点 |
|------|---------|---------|---------|
| B19 | 手机号无格式校验 | 个人档案和成员绑定手机号增加 4-12 位数字校验 | 输入不合法手机号提示并清空 |
| B20 | 出生日期只能用选择器，无法手动输入 | 改为 input 数字输入 + 📅 选择器双模式，年限定 1900-当前年 | 可手动输入也可点图标选择，超出范围自动修正 |
